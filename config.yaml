# AI Feeds Configuration

# Feed sources
sources:
  arxiv:
    enabled: true
    categories:
      - cs.AI
      - cs.CL
      - cs.LG
      - stat.ML
    max_results: 150
    delay_seconds: 3.0
    days_back: 2
    timeout_seconds: 30
    retries: 3

  huggingface:
    enabled: true
    limit: 30
    days_back: 2
    timeout_seconds: 30
    retries: 3
    delay_seconds: 1.0

  hackernews:
    enabled: true
    queries:
      - "AI"
      - "LLM"
      - "machine learning"
    max_stories: 30
    timeout_seconds: 30
    retries: 3

  reddit:
    enabled: true
    subreddits:
      - MachineLearning
      - LocalLLaMA
      - artificial
    sort: hot
    limit: 25
    timeout_seconds: 30
    retries: 3
    delay_seconds: 1.0

  github_trending:
    enabled: true
    languages:
      - python
      - typescript
    since: daily
    timeout_seconds: 30
    retries: 3
    delay_seconds: 2.0
    max_results: 30

  producthunt:
    enabled: false  # Requires OAuth2
    topics:
      - artificial-intelligence

  devto:
    enabled: true
    tag: ai
    top: 7
    timeout_seconds: 30
    retries: 3
    delay_seconds: 1.0
    limit: 30

# Processing
processor:
  llm:
    provider: claude  # or openai, ollama
    model: claude-sonnet-4-20250514
  relevance_threshold: 7  # Only surface items scoring 7+
  max_items_per_source: 10

# Database
database:
  path: db/ai-feeds.sqlite

# Output
output:
  vault_path: ~/vault/knowledge/wikis/ai-engineering/raw/signals
  format: obsidian  # obsidian, markdown, json

# Learning plan integration
learning_plan:
  interests:
    - context engineering
    - agent architectures
    - RAG
    - fine-tuning
    - production AI
    - LLM integration
    - browser automation
  unchecked_modules: 18
  phase: 4
